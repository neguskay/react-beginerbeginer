export default function productReducer(state = [], {type, payload}){
    //console.log(action)
    // action can be modelled as an object with type and paload
    return state;
}

//productReducer;

//Note always export default on the funcxtion line if only exporting sayd function else
// wrap it in an objevct and export the object